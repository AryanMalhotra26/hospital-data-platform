/**
 * PatientsPage — placeholder for Step 4. The real patient list/create/edit UI
 * (wired to /api/patients with the Bearer token) arrives in Step 5.
 */

export default function PatientsPage() {
  return (
    <div>
      <h1 className="page__title">Patients</h1>
      <div className="placeholder">
        Patient management UI comes in <strong>Step 5</strong>. The protected route + nav are working —
        this page only renders because you hold a valid token.
      </div>
    </div>
  );
}
