# Optional. The base URL of the backend API.
# If unset, the client defaults to http://localhost:4000/api (see src/api/client.js).
# Vite only exposes vars prefixed with VITE_ to the browser.
VITE_API_URL=http://localhost:4000/api
